# 模型片段：qwen3.5-9b
# 作为实验「多继承」的一项，覆盖基底里的模型相关字段。本身不含 defaults，是个 partial。
policy:
  model_name: "Qwen/Qwen3.5-9B"
  tokenizer:
    name: "Qwen/Qwen3.5-9B"
  # 9B 更吃显存：可在此设较保守的默认（实验仍可再覆盖）
  # max_total_sequence_length: 4096
  # dtensor_cfg:
  #   activation_checkpointing: true
