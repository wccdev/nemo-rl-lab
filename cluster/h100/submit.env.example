# 提交配置·集群专属层 —— 单机 1× H100（profile: h100）。随集群走，只放「连哪台机 + 容器内路径」。
#   cp cluster/h100/submit.env.example cluster/h100/submit.env   # 改 IP 即可
# 通用层（密钥 / RUN_USER）见 cluster/submit.env（cp cluster/submit.env.example cluster/submit.env）。
#
# 用法（全程在自己机器上、经 VPN 连到 H100 机器，无需 SSH 进容器）：
#   1) 连 VPN
#   2) uv run lab submit <实验名>          # 实验自带 cluster=h100 即用本文件的地址；本仓库代码随作业上传
#   3) uv run lab job logs <job_id> -f     # 实时日志
# 详见 docs/remote-submit.md。

# ---------- 连哪台机 ----------
# H100 机器上 Ray head 的 dashboard 地址（VPN 内可达；端口默认 8265）。多台机器改成你被分配那台的 VPN IP。
RAY_DASHBOARD_ADDRESS=http://x.x.x.x:8265
# 容器内 NeMo-RL 0.6.0 的路径（容器内路径，不是你本机路径）。
NEMO_RL_DIR=/opt/nemo-rl

# ---------- 远程编排 / 隧道（可选；lab tunnel / lab cluster up|down 用）----------
# 不同网段连不上 8265 时：lab tunnel 用 ssh 把端口转发到本机（然后 RAY_DASHBOARD_ADDRESS 改 http://127.0.0.1:8265）。
# SSH_HOST=h100-host                  # ssh 目标（跳板机 / H100 机器）
# RAY_HEAD_IP=172.19.12.24            # head 节点 IP（默认从 RAY_DASHBOARD_ADDRESS 解析）
# 单机 H100 一般在容器内 lab ray head 即可；若要从本机远程起：
# CLUSTER_SSH_HEAD=h100-host          # head 节点 ssh 目标
# CLUSTER_CONTAINER=nemo-rl           # 容器名
# CLUSTER_REPO_DIR=/work/llm          # 容器内仓库路径

# ---------- 产物落盘（强烈建议设，否则落在 Ray 临时目录会丢）----------
# checkpoint + 每步样本 jsonl + 日志落到 OUTPUT_ROOT[/<RUN_USER>]/<实验名>；指到集群持久路径/共享盘。
OUTPUT_ROOT=/data/nemo-rl-runs

# ---------- 集群侧密钥文件（推荐，多人共用必备）----------
# 容器内一份密钥文件（KEY=VALUE，含 SWANLAB_API_KEY / HF_TOKEN / JUDGE_API_KEY / KB_API_KEY）。
# 设了它：submit 仅转发本路径、密钥不再明文进 Ray Dashboard；run.sh 在集群侧 source 它。
# 文件请放在持久且仅本人可读的位置（chmod 600），不要放进随作业上传的目录。
# CLUSTER_SECRETS_FILE=/data/secrets/nemo-rl.env

# ---------- 模型缓存（按需）----------
# 容器内模型缓存目录（须先 prefetch 或集群能直连 huggingface.co）。
HF_HOME=/data/hf_cache

# ---------- 内部数据集（留在集群、不上传）----------
# 被 .gitignore 命中的内部数据（如题库 datasets/qa_rl/）不随作业上传，须先放到集群，
# 再在此指向【容器内绝对路径】，submit 会转发并覆盖自动推导。
QA_RL_DATA_DIR=/data/datasets/qa_rl

# ---------- 简答题裁判 LLM / RAGFlow 检索（仅 qa 系列实验用，按需开）----------
# 判分/检索在集群训练进程里发生，地址须从集群容器可达。完整字段见 cluster/gb10-spark/submit.env.example。
# JUDGE_BASE_URL=http://x.x.x.x:8001/v1
# JUDGE_MODEL=Qwen/Qwen2.5-7B-Instruct
# JUDGE_API_KEY=EMPTY
# KB_BASE_URL=http://x.x.x.x:9380
# KB_API_KEY=ragflow-xxxxx
# KB_DATASET_IDS=id1,id2
